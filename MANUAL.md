# 🛡️ 忍 SHINOBI - 究極個人認証システム 完全設計・取扱説明書 (v3.4)

本システムは、Windows 11 Pro 環境において、OSの標準ログイン環境を秘匿し、物理層からアプリケーション層までの多層防壁を構築する、妥協なきセキュリティ・ソリューションです。

---

## 1. 認証アーキテクチャ (v3.4)

### 1.1 MFA 解錠ルール
厳格モード（Strict Mode）では、セキュリティと利便性のバランスを考慮し、以下の条件でシステムが解錠されます。
1.  **スマホ鍵 (MOBILE):** 1要素のみで解錠可能。
    *   QRペアリング済みスマホから解錠信号を送信した瞬間に解錠されます。
2.  **その他の要素:** **任意の 2 つ** がクリアされた瞬間に解錠。
    *   顔認証 (FACE)
    *   スマホ接近 (BT PROXIMITY)
    *   マスター PIN (SHA-256)

### 1.2 スマート緩和プロトコル
*   解錠から設定時間内であり、かつ Bluetooth デバイスが近傍にある場合は、上記どの要素であっても **1 要素のみ** で即解錠が可能です。

---

## 2. セットアップ・ガイド (Initial Setup)

初回起動時に統合ウィザードが自動開始されます。
1.  **MASTER PIN:** 4 桁以上の数字を設定。
2.  **FACE SCAN:** カメラによる生体プロファイルの登録。
3.  **MOBILE PAIRING:** 画面に表示される **QR コード** をスマホでスキャン。これにより解錠用トークンが同期されます。

---

## 3. 管理者コンソールと救済措置

### 3.1 管理者コンソール
解錠後、サイドバーから設定の微調整、リアルタイムモニター、監査ログのエクスポート、**スマホ鍵の再発行 (RESET)** が可能です。

### 3.2 緊急用解錠 (EMERGENCY_BYPASS)
ロック画面のボタンから呼び出し可能。
*   マスター PIN の入力 ＋ **証拠用顔写真の強制記録** を条件に、単独でシステムを解錠します。

---

## 4. リカバリ手順

システムが動作しなくなった場合：
1.  **セーフモード** で起動。
2.  レジストリ \`HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon\\Shell\` を \`explorer.exe\` に戻す。
3.  BitLocker 回復キーは必ず物理的に保管しておいてください。

---
**⚠️ 免責事項**
本ソフトウェアは高度なシステム制御を行います。使用は自己責任でお願いします。
EOF
