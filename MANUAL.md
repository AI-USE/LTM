# 🛡️ 忍 SHINOBI - 究極個人認証システム 取扱説明書 (v4.1)

SHINOBI は、Windows OS の深部からハードウェア連携までを掌握し、PC を「本人以外にはただの文鎮」に変える、妥協なき究極セキュリティ・スイートです。

---

## 1. セキュリティ・アーキテクチャ (v4.1)

### 1.1 多層防壁
1.  **物理層: BitLocker (TPM 2.0)** - ドライブ全体を AES-256 で暗号化。外部解析を完全遮断。
2.  **OS 層: Custom Shell (Winlogon)** - デスクトップを隠蔽し、SHINOBI を唯一の操作インターフェースとする。
3.  **権限層: Registry Hardening** - 管理ツールやロックホットキーを動的に無効化。
4.  **監視層: Double-Guard Watchdog** - メインプログラムと監視プロセスが相互監視し、0.5秒以内に再起動。
5.  **認証層: Parallel MFA Engine** - 顔・BT・PIN・スマホ鍵のインテリジェント並列判定。

### 1.2 認証ルール (厳格モード)
以下のいずれかの条件を満たすとシステムが解錠されます。
*   **スマホ鍵:** QRペアリング済みスマホからの1操作。
*   **並列MFA:** 以下のうち **任意の 2 つ** が同時クリア。
    *   **顔認証:** 1:N 生体照合。
    *   **スマホ接近:** 指定デバイスの Bluetooth 距離判定。
    *   **マスター PIN:** SHA-256 保護された暗証番号。

---

## 2. 導入とセットアップ

初回起動時に「究極のセットアップ・ウィザード」が全 6 ステップでガイドします。
1.  **システム診断:** 管理者権限と BitLocker 状態の検証。
2.  **マスター PIN:** 全防衛の基底となる PIN の設定。
3.  **生体登録:** カメラによる精密な顔プロファイルの作成（複数回スキャン）。
4.  **近接設定:** 連携スマホの Bluetooth スキャンと感度調整。
5.  **スマホ鍵同期:** トークンの QR 同期。
6.  **権限委譲:** カスタムシェルの登録許可。

---

## 3. 管理者コンソール (Admin PRO)

解錠後、サイドバーから高度な管理が可能です。
*   **状況監視:** RSSI 値や顔合致距離を数値でリアルタイム監視。
*   **OS 硬化:** レジストリ制限の個別トグルスイッチ。
*   **感度調整:** 環境に合わせた各センサーの精密キャリブレーション。
*   **監査ログ:** ログイン時の「現場写真」付きログの確認と CSV 出力。

---

## 4. 緊急救済とリカバリ

### 4.1 救済ルート (Rescue Route)
ロック画面のボタンから、マスター PIN の入力 ＋ **有効な顔の検知（強制撮影）** を条件に緊急解錠。

### 4.2 セーフモード復旧
不具合時は **セーフモード** で起動し、レジストリ \`Winlogon\\Shell\` を \`explorer.exe\` に戻してください。

---
**⚠️ 免責事項**
本システムは高度な OS 制御を行います。回復キー等の物理情報は必ず別途保管してください。
EOF
