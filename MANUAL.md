# 🛡️ 忍 SHINOBI - 究極個人認証システム 完全設計・取扱説明書 (v3.2)

本システムは、Windows 11 Pro 環境において、OSの標準ログイン環境を秘匿し、物理層からアプリケーション層までの多層防壁を構築する、妥協なきセキュリティ・ソリューションです。

---

## 1. 究極の MFA エンジン (Parallel Auth)

### 1.1 同時待機・並列判定
SHINOBI v3.2 では、全ての認証要素がバックグラウンドで常に待機しています。厳格モードにおいては、以下の要素のうち**任意の 2 つ**が「CLEAR」になった瞬間に解錠されます。
*   **顔認証 (FACE):** 登録済みの顔を検知。
*   **スマホ接近 (BT):** 指定された Bluetooth デバイスが近距離にある。
*   **スマホ鍵 (WEB):** ブラウザから送信された認証トークンを検証。
*   **マスター PIN:** 正しい暗証番号を入力。

### 1.2 スマート緩和プロトコル
*   解錠から設定時間内であり、かつ Bluetooth デバイスが近傍にある場合は、**1 要素のみ**で即解錠が可能です。

---

## 2. 強固な防衛機能 (Defense Layers)

### 2.1 OS 封鎖
*   **Winlogon Shell:** デスクトップを隠蔽。
*   **Registry Injection:** タスクマネージャー、Win+L、ログオフ等をレジストリレベルで封じます。
*   **Keyboard Hook:** Alt+Tab, Alt+F4, Win キー等の回避入力を無効化。

### 2.2 PIN 時間ロック (Lockout)
*   PIN入力を 3 回連続で間違えると、段階的に入力が禁止されます（30秒、60秒...）。ロックアウト中はタイマーが秒単位で表示されます。

---

## 3. セットアップ・ガイダンス (Guided Setup)

初回起動時に統合ウィザードが開始されます：
1.  **マスター PIN 設定:** SHA-256 ハッシュによる保護。
2.  **顔スキャン:** 実際のカメラを用いた特徴量登録。
3.  **BT デバイス選択:** 連携するスマートフォンのペアリング設定。
4.  **スマホ鍵同期:** 解錠トークンの発行とスマホ側への設定。

---

## 4. 管理者コンソールと救済措置

### 4.1 管理者コンソール
解錠後、サイドバーから設定の微調整、リアルタイムモニターの確認、監査ログの CSV エクスポートが可能です。

### 4.2 緊急用解錠 (EMERGENCY_BYPASS)
通常の MFA フローから独立した救済ルートです。
*   ロック画面の専用ボタンから起動。
*   マスター PIN の入力と**証拠用顔写真の強制記録**を条件に解錠されます。

---

## 5. リカバリ手順

バグ等で動作しなくなった場合：
1.  **セーフモード**で起動。
2.  レジストリ `HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon\Shell` を `explorer.exe` に戻す。
3.  BitLocker 回復キーは必ず物理的に保管しておいてください。

---
**⚠️ 免責事項**
本ソフトウェアは高度なシステム制御を行います。使用は自己責任でお願いします。
