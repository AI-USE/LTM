# 🛡️ 忍 SHINOBI - 究極個人認証システム 取扱説明書 (v3.4)

本システムは、Windows 11 Pro 環境において、OSの標準ログイン環境を秘匿し、物理層からアプリケーション層までの多層防壁を構築する、妥協なきセキュリティ・ソリューションです。

---

## 1. システム概要 (Architecture)

### 1.1 多層防御レイヤー
1.  **物理層: BitLocker (TPM 2.0)** - ドライブ全体を暗号化。外部からのデータ解析を封鎖。
2.  **OS 層: カスタムシェル (Winlogon)** - `explorer.exe` を停止させ、認証までデスクトップを秘匿。
3.  **権限層: レジストリ制御** - タスクマネージャー、Win+L、ログアウト等をレジストリレベルで封殺。
4.  **監視層: キーボードフック** - Alt+Tab, Alt+F4 等の回避コマンドを物理的に遮断。
5.  **認証層: MFA エンジン** - 顔・BT・PIN・スマホ鍵を組み合わせたインテリジェント判定。

---

## 2. 認証ルール (Authentication)

### 2.1 解錠ルート
厳格モード（Strict Mode）では、以下のいずれかの条件を満たすと解錠されます。
*   **スマホ鍵 (最優先):** QRペアリング済みスマホからの1操作で即解錠。
*   **並列MFA:** 以下の要素のうち **任意の 2 つ** が揃うと解錠。
    *   **顔認証:** カメラによる生体照合。
    *   **スマホ接近:** Bluetooth の電波強度による距離判定。
    *   **マスター PIN:** SHA-256 で保護された暗証番号。

### 2.2 セキュリティ・ロックアウト
*   PIN入力を 3 回連続で間違えると、段階的なペナルティ（30秒〜）が発生し、入力が遮断されます。

---

## 3. 導入手順 (Setup)

1.  **初回起動:** セットアップ・ウィザードが自動開始されます。
2.  **マスターPINの設定:** 4桁以上の番号を登録。
3.  **生体情報の登録:** カメラで顔の特徴をスキャン。
4.  **スマホ鍵の同期:** 画面の **QR コード** をスマホで読み取り。

---

## 4. 管理者コンソール (Admin Console)

解錠後、サイドバーから以下の操作が可能です。
*   **リアルタイム監視:** システム稼働状況の確認。
*   **セキュリティ設定:** 各種センサーの閾値調整、スマホ鍵の再発行。
*   **監査ログ:** 過去7日間の履歴確認とエクスポート。

---

## 5. リカバリ手順 (Emergency)

万が一、解錠できなくなった場合：
1.  PCを **セーフモード** で起動。
2.  レジストリ \`HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon\\Shell\` を \`explorer.exe\` に書き戻す。
3.  BitLocker の回復キーは必ず物理的に保管しておいてください。

---
**⚠️ 免責事項**
本システムは高度な OS 制御を行います。使用は自己責任でお願いします。
EOF
