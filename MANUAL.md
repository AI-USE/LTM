# 🛡️ 忍 SHINOBI - 究極個人認証システム 完全設計・取扱説明書 (v3.3)

本システムは、Windows 11 Pro 環境において、OSの標準ログイン環境を秘匿し、物理層からアプリケーション層までの多層防壁を構築する、妥協なきセキュリティ・ソリューションです。

---

## 1. 認証アーキテクチャ (v3.3)

### 1.1 並列 MFA エンジン
厳格モードでは、以下の要素のうち **任意の 2 つ** が揃った瞬間にシステムが解錠されます。
*   **顔認証 (FACE):** カメラによる 1:N 高速照合。
*   **スマホ接近 (BT):** Bluetooth RSSI 強度による距離判定。
*   **スマホ鍵 (MOBILE):** QRペアリング済みスマホからの BLE 解錠信号。
*   **マスター PIN:** SHA-256 ハッシュ化された暗証番号。

### 1.2 PIN セキュリティ・ロックアウト
*   3 回連続で PIN を間違えると、段階的なペナルティ（30 秒〜）が発生し、物理的に入力が遮断されます。ロックアウト中は残り時間が秒単位で表示されます。

---

## 2. セットアップ・ガイド (Initial Setup)

初回起動時に統合ウィザードが自動開始されます。
1.  **MASTER PIN:** 4 桁以上の数字を設定。
2.  **FACE SCAN:** カメラによる生体プロファイルの登録。
3.  **MOBILE PAIRING:** 画面に表示される **QR コード** をスマホでスキャン。これにより解錠用トークンが同期されます。
4.  **MOBILE SETUP:** スマホ側で \`index.html\` を開き、スキャンした設定を保存してください。

---

## 3. 管理者コンソール (Admin Interface)

解錠後、サイドバーから高度なカスタマイズが可能です。
*   **MONITORING:** センサー負荷、システム時刻、ステータスのリアルタイム監視。
*   **CONFIGURATION:**
    *   各センサーの閾値調整。
    *   **RESET_MOBILE_KEY:** スマホ鍵を紛失した際や、定期的な更新のために新しい鍵を再発行します。
*   **AUDIT_LOGS:** 過去 7 日間の全ログイン履歴の閲覧と CSV エクスポート。

---

## 4. 緊急時の救済措置

### 4.1 EMERGENCY_BYPASS
ロック画面のボタンから呼び出し可能。
*   マスター PIN の入力 ＋ **証拠用顔写真の強制記録** を条件に、単独でシステムを解錠します。

### 4.2 OS リカバリ
万が一の不具合時は、PC を **セーフモード** で起動し、レジストリから \`Winlogon\\Shell\` を \`explorer.exe\` に書き戻してください。

---

## 5. 付録: スマホ鍵 (Mobile Key) の運用

1.  スマホのブラウザで \`SHINOBI/assets/mobile_key/index.html\` を開く。
2.  初回のみ、PC のウィザードに表示された設定（JSON）を貼り付けて保存。
3.  以降は、PC の前で「PC を解錠する」ボタンを押すだけで Bluetooth 経由で信号が飛びます。

---
**⚠️ 免責事項**
本システムは高度な OS 制御を行います。BitLocker 回復キーは必ず物理的に保管してください。
EOF
