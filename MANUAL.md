# 🛡️ 忍 SHINOBI - 究極個人認証システム 取扱説明書 (v3.5 PRO)

SHINOBI は、Windows 11 Pro 環境を「本人以外にはただの文鎮」に変え、かつ「本人がいる間は最高に快適」にするための、妥協なき究極セキュリティ・スイートです。

---

## 1. コア・アーキテクチャ

### 1.1 多層防壁 (Defense-in-Depth)
1.  **物理層: BitLocker (TPM 2.0)** - ドライブ全体を暗号化し、外部解析を封鎖。
2.  **OS 層: Custom Shell (Winlogon)** - デスクトップそのものを秘匿し、SHINOBI を唯一の入り口とする。
3.  **権限層: Registry Injection** - 管理ツール（TaskMgr等）やシステムコマンド（Win+L等）を動的に無効化。
4.  **監視層: Low-level Hooks** - 回避用キー入力を物理レベルで検知・遮断。
5.  **認証層: Parallel MFA Engine** - 任意の 2 要素クリアで解錠するインテリジェント判定。

---

## 2. 導入・セットアップ手順

本システムは、初回起動時に「究極のセットアップ・ウィザード」が開始されます。
1.  **BitLocker 確認:** ドライブの保護状態を確認します（有効化を強く推奨）。
2.  **マスター PIN:** 4 桁以上の数字を登録。SHA-256 でハッシュ化保存されます。
3.  **生体登録:** カメラで顔の特徴量をスキャンし、暗号化保存します。
4.  **近接設定:** Bluetooth デバイスを選択し、検知感度（RSSI）を調整します。
5.  **スマホ鍵同期:** QR コードをスキャンし、最優先解錠トークンをスマホへ同期します。
6.  **シェル登録:** デスクトップ置換の許可を行い、防衛をオンラインにします。

---

## 3. 運用・解錠ルール

### 3.1 認証ルート
*   **スマホ鍵 (Priority):** スマホからの 1 操作で即座に解錠。
*   **並列 MFA (Strict):** 以下のうち **任意の 2 つ** が揃うと自動解錠。
    *   登録済みの顔の検知。
    *   指定スマホの Bluetooth 接近検知。
    *   マスター PIN の入力。

### 3.2 セキュリティ・ロックアウト
*   PIN入力を 3 回連続で間違えると、段階的なペナルティ（30秒〜）が発生し、入力が遮断されます。

---

## 4. 管理者ダッシュボード (PRO)

解錠後、以下の高度な管理が可能です。
*   **STATUS:** システムの稼働時間と心拍監視のリアルタイム表示。
*   **HARDENING:** レジストリ制限やキーボードフックの個別・即時適用。
*   **SENSORS:** 閾値（感度・厳格度）の精密なキャリブレーション。
*   **AUDIT:** 過去 7 日間の顔写真付きログの確認と CSV エクスポート。

---

## 5. 緊急時のリカバリ

1.  PC を **セーフモード** で起動。
2.  レジストリ \`HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon\\Shell\` を \`explorer.exe\` に書き戻す。
3.  BitLocker 回復キーは必ず物理的に（紙等で）別保管しておいてください。

---
**⚠️ 免責事項**
本ソフトウェアは高度なシステム制御を行います。BitLocker 回復キーの紛失等によるデータ損失について、開発者は一切の責任を負いません。
EOF
