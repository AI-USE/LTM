# 🛡️ 忍 SHINOBI - 究極個人認証システム 完全設計・取扱説明書 (v3.1)

本システムは、Windows 11 Pro 環境において、OSの標準ログイン環境を秘匿し、物理層からアプリケーション層までの多層防壁を構築する、妥協なきセキュリティ・ソリューションです。

---

## 1. システム・アーキテクチャ (Architecture)

### 1.1 多層防御レイヤー
1.  **物理層: BitLocker (TPM 2.0)**
    *   ドライブ全体を AES-256 ビットで暗号化。
    *   TPM 2.0 チップと連携し、構成変化（セーフモード移行、ハードウェア変更）を検知すると即座に回復キーを要求。
    *   **役割:** SSD 抜き出しや別 OS からの解析を完全封鎖。
2.  **OS 層: Custom Shell (Winlogon)**
    *   `explorer.exe` を停止させ、本システムを唯一のシェルとしてレジストリ登録。
    *   **役割:** デスクトップ空間そのものを認証成功まで秘匿。
3.  **権限層: Registry Injection**
    *   ロック中、タスクマネージャー、Win+L、パスワード変更、サインアウトを動的に無効化。
    *   **役割:** 標準的なバイパス手段をレジストリレベルで封殺。
4.  **監視層: Low-level Keyboard Hook**
    *   `WH_KEYBOARD_LL` を用い、Alt+Tab, Alt+F4, Win キーの入力を検知した瞬間に破棄。
    *   **役割:** ウィンドウ切り替えや強制終了による回避を物理的に遮断。
5.  **認証層: MFA Engine**
    *   顔・BT・PIN・スマホ鍵を組み合わせた 5 大解錠ルートの判定。
    *   **役割:** 利便性と鉄壁のセキュリティを両立。

---

## 2. 認証ロジックと解除ルート (Authentication Routes)

### 2.1 厳格モード (Strict Mode) - 通常解除
以下のいずれかの組み合わせが揃った瞬間に解錠し、`explorer.exe` を呼び出します。
1.  **ステルス:** 顔認証 ＋ スマホ BT 接近 (座るだけで解錠)
2.  **ハイブリッド:** PIN ＋ スマホ BT 接近 (暗所やマスク時)
3.  **ブラウザ鍵:** スマホで生体認証 (WebAuthn) ⇒ Bluetooth 送信 (スマホ操作で解錠)
4.  **W 認証:** 顔認証 ＋ PIN (スマホ紛失・電池切れ時)
5.  **救済登録:** PIN ＋ 顔スキャン (緊急用。顔写真を証拠として記録し解錠)

### 2.2 スマート緩和プロトコル (Smart Mitigation)
*   **緩和条件:** 前回解錠から設定時間（デフォルト 1 時間）以内、かつ BT 生存確認に成功している場合。
*   **挙動:** 1 要素（顔 or BT or PIN のどれか）のみで即解錠。
*   **心拍監視 (Heartbeat):** 設定間隔（デフォルト 10 分）ごとに Bluetooth RSSI を測定。スマホが離れた（基準値以下）場合、即座に「厳格モード」へ格上げし、画面をロック。

---

## 3. セットアップ・ガイダンス (Setup Guidance)

本システムは初回起動時に自動的にセットアップ・ウィザードが開始されます。
1.  **ステップ 1: マスター PIN 設定**
    *   4 桁以上の PIN を設定。入力値は SHA-256 でハッシュ化され安全に保存されます。
2.  **ステップ 2: 顔データの登録**
    *   正面を向き、特徴量をスキャン。マルチサンプル対応で、複数の表情を登録可能。
3.  **ステップ 3: システム整合性チェック**
    *   BitLocker の動作状態、管理者権限の有無を自動検証。

---

## 4. 管理者コンソール (Admin Console)

解錠後、ダッシュボードから以下の設定が可能です：
*   **MONITORING:** リアルタイムの時刻、システムステータス、センサー負荷を表示。
*   **CONFIGURATION:**
    *   Bluetooth 接近閾値（dBm）の調整。
    *   顔認証精度（しきい値）の調整。
    *   スマート緩和時間（1〜12 時間）の設定。
    *   生存確認間隔（1〜60 分）の設定。
*   **AUDIT_LOGS:**
    *   過去 7 日間の全ログイン成功時の「顔写真」と「ルート」を保存。
    *   CSV 形式へのエクスポート機能。

---

## 5. リカバリと安全対策 (Safety & Recovery)

物理緊急キーを廃止したため、以下の安全策を講じています：
*   **二重監視 (Watchdog):** プロセスがクラッシュした場合、0.5 秒以内に再起動・再ロック。
*   **構成バックアップ:** 設定ファイルは自動的に `config_backup.json` に二重化保存。
*   **緊急リカバリ:** システムがバグ等で動かなくなった場合は、**セーフモードで起動**し、レジストリから `Shell` を `explorer.exe` に戻してください。

---

## 6. 付録: スマホ鍵 (Browser Key) の使い方

1.  PC 側で `browser_key.py` を起動し、待機状態にします。
2.  スマホのブラウザで `SHINOBI/assets/mobile_key/index.html` を開きます。
3.  「指紋・顔で解錠」をタップ。
4.  認証成功後、PC へ Bluetooth 経由でトークンが送信され、自動解錠されます。

---

**⚠️ 免責事項**
本システムは PC の操作を強力に制限します。管理者パスワードや BitLocker 回復キーは必ず物理的に保管してください。本ソフトウェアに起因するデータ損失や操作不能状態について、開発者は一切の責任を負いません。
EOF
